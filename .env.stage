# ── Staging environment — committed, non-secret values only ──────────────────
# Secrets (AZURE_*) are injected at runtime via CI/CD or container env vars.
# Local overrides go in .env.stage.local (gitignored).

APP_ENV=stage
APP_DEV_URL=https://dev.mission-control.example.com
APP_STAGE_URL=https://stage.mission-control.example.com
APP_PROD_URL=https://mission-control.example.com
